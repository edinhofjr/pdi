{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake build clean",
            "type": "shell",
            "command": "rm -rf build && mkdir build && cd build && cmake .. && cmake --build .",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
